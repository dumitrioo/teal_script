global_init() {
    if(this.initialized == undefined) {
        this.initialized = true;
        set_cycle_nanosleep(1000000);
    }
}

global_init glbl_ini();

my_drafts() {
    println("drafts");
    return exit(0);
}

my_drafts drf();
